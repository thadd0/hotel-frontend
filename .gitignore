# dependencias
node_modules/

# build
dist/

# variables de entorno (NUNCA subir al repo)
.env
.env.local
# .env.production NO va en gitignore — solo tiene VITE_API_URL= (vacío, sin secretos)
# y es necesario que exista en el repo para que Docker lo use al compilar

# archivos del editor
.vscode/
.idea/

# sistema
.DS_Store
Thumbs.db
